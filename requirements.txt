# ==========================================
# VoC Platform · 本地一键安装（全量）
# ==========================================
# 包含核心 + ML + 仪表盘；本地 `pip install -r requirements.txt` 一次性装齐。
# CI / 测试请用 requirements-core.txt（避免 torch/transformers 拖慢）。

# 核心 + ML + 仪表盘 三组合并
-r requirements-core.txt
-r requirements-ml.txt
-r requirements-dashboard.txt

# --- 开发与代码风格（仅本地用） ---
pytest-cov>=4.1.0
black>=23.12.0
ruff>=0.1.6